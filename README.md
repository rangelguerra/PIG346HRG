git status //Verás archivos modificados y/o nuevos.
git init
git add . //Agregar todo lo que cambió
git commit -m "Agrega ejercicio de variables"
git push
https://github.com/rangelguerra/PIG346HRG
Cada vez que hagas cambios:
    git status
    git add .
    git commit -m "mensaje claro del cambio"
    git push

⚠️ Si GitHub te vuelve a decir “fetch first”
    git pull --rebase
    # resolver conflictos si aparecen
    it push

🛡️ Buen hábito
Antes de empezar a trabajar
    git pull

